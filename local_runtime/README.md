# Local Runtime Modules

This directory contains project-specific grammars, mappings, fixtures, and task
helpers that are intentionally kept outside the public `bots_airflow` package.

These files are available for local imports in this workspace, but they are not
part of the published `bots-airflow` wheel or sdist.

